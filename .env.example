# Backend API base URL used by shared apiClient.
# Vite exposes only VITE_* variables to browser code.
VITE_API_URL=http://localhost:3000

# NAVER Maps Dynamic Map client ID (provided as a GitHub Secret).
VITE_NAVER_MAP_CLIENT_ID=

# Kakao JavaScript app key (Kakao share SDK).
VITE_KAKAO_APP_KEY=

# OAuth client IDs for social login.
VITE_KAKAO_CLIENT_ID=
VITE_NAVER_CLIENT_ID=
VITE_GOOGLE_CLIENT_ID=
VITE_APPLE_CLIENT_ID=

# Figma REST 덤프용 (로컬 도구 전용 — VITE_ 접두사 금지: 클라이언트 번들에 노출 방지).
# 토큰은 File content(read) 권한으로 발급, 작업 후 revoke 권장.
FIGMA_TOKEN=
# 파일 URL의 figma.com/design/<KEY>/... 에서 KEY 부분.
FIGMA_FILE_KEY=
# (선택) 특정 프레임만 받을 때. "Copy link to selection"의 node-id 값.
FIGMA_NODE_ID=
